#include "main.h"

/**
  * mul - this multiplies two numbers
  * @params: params to be multiplied
  * Return: return (multiplied result)
  */

int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
