#include "main.h"

/**
  * mul - this multiplies two numbers
  * @a: first number to be multiplied
  * @b: second number to be multiplied
  * Return: return (multiplied result)
  */

int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
