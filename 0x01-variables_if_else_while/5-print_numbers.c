#include <stdio.h>

/**
 * main - initialization of to print numbers
 * Return: return 0
 */

int main(void)
{
	int num;

	while (num < 10)
	{
		printf("%d", num);
		num++;
	}
	printf("\n");
	return (0);
}
